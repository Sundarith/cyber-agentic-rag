\documentclass[tikz,border=8pt]{standalone}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{xcolor}
\usetikzlibrary{arrows.meta,positioning,fit,calc,decorations.pathreplacing,backgrounds}
\pgfdeclarelayer{background}
\pgfsetlayers{background,main}

\definecolor{ink}{HTML}{334155}
\definecolor{line}{HTML}{64748B}
\definecolor{stagefill}{HTML}{F8FAFC}
\definecolor{datablue}{HTML}{DCEAF8}
\definecolor{agentorange}{HTML}{F6B44B}
\definecolor{knowledgegreen}{HTML}{DFF3E8}
\definecolor{verifyviolet}{HTML}{EDE9FE}
\definecolor{evalred}{HTML}{FDECEC}
\definecolor{sourcecream}{HTML}{FFF4CC}

\tikzset{
  font=\sffamily,
  stage/.style={
    draw=line,
    line width=0.65pt,
    fill=stagefill,
    inner sep=5mm
  },
  stage title/.style={
    font=\sffamily\bfseries\large,
    text=black
  },
  box/.style={
    draw=line,
    line width=0.65pt,
    fill=datablue,
    align=center,
    minimum height=13mm,
    text width=33mm,
    inner sep=3pt
  },
  widebox/.style={
    box,
    text width=43mm
  },
  small/.style={
    font=\sffamily\footnotesize,
    text=ink
  },
  orange/.style={box, fill=agentorange},
  cream/.style={box, fill=sourcecream},
  green/.style={box, fill=knowledgegreen},
  violet/.style={box, fill=verifyviolet},
  redbox/.style={box, fill=evalred},
  arrow/.style={
    -{Latex[length=2.4mm]},
    draw=ink,
    line width=0.75pt
  },
  feedback/.style={
    -{Latex[length=2.4mm]},
    draw=ink,
    dashed,
    line width=0.75pt
  },
  side label/.style={
    rotate=90,
    font=\sffamily\Large,
    text=ink,
    align=center
  }
}

\begin{document}
\begin{tikzpicture}[node distance=6mm and 6mm]

% ---------------------------------------------------------------------------
% 1. Query planning and tool policy
% ---------------------------------------------------------------------------
\node[cream] (q) at (0,0) {User / Benchmark\\Query\\{\small CVE, CWE, CAPEC, ATT\&CK}};
\node[box, right=of q] (decompose) {Decompose into\\Subquestions};
\node[orange, right=of decompose] (planner) {Query Planner\\and Tool Policy};
\node[box, right=of planner] (budget) {Tool Budget\\Stopping Criteria};

\begin{pgfonlayer}{background}
\node[stage, fit=(q)(decompose)(planner)(budget), label={[stage title]above:{1. Query Planning and Agent Control}}] (stage1) {};
\end{pgfonlayer}

\draw[arrow] (q) -- (decompose);
\draw[arrow] (decompose) -- (planner);
\draw[arrow] (planner) -- (budget);

% ---------------------------------------------------------------------------
% 2. Agentic evidence retrieval
% ---------------------------------------------------------------------------
\node[box, below=16mm of q] (search) {Hybrid Search\\{\small BM25 + dense vectors}};
\node[box, right=of search] (open) {Open / Find / Quote\\{\small exact IDs and sections}};
\node[green, right=of open] (substrate) {Knowledge Substrate\\{\small hierarchy + CTI graph}};
\node[box, right=of substrate] (rerank) {Rerank and Compare\\Candidate Evidence};

\begin{pgfonlayer}{background}
\node[stage, fit=(search)(open)(substrate)(rerank), label={[stage title]above:{2. Agentic Evidence Retrieval}}] (stage2) {};
\end{pgfonlayer}

\draw[arrow] (budget.south) |- (search.north);
\draw[arrow] (search) -- (open);
\draw[arrow] (open) -- (substrate);
\draw[arrow] (substrate) -- (rerank);

% ---------------------------------------------------------------------------
% 3. Verification and grounded synthesis
% ---------------------------------------------------------------------------
\node[violet, below=16mm of search] (verify) {Evidence Verifier\\{\small support and sufficiency checks}};
\node[box, right=of verify] (repair) {Missing Evidence?\\{\small revise search plan}};
\node[orange, right=of repair] (synth) {Grounded Synthesizer\\{\small answer with citations}};
\node[redbox, right=of synth] (answer) {Final Answer\\{\small auditable citations}};

\begin{pgfonlayer}{background}
\node[stage, fit=(verify)(repair)(synth)(answer), label={[stage title]above:{3. Verification and Grounded Answer Generation}}] (stage3) {};
\end{pgfonlayer}

\draw[arrow] (rerank.south) |- (verify.north);
\draw[arrow] (verify) -- (repair);
\draw[arrow] (repair) -- (synth);
\draw[arrow] (synth) -- (answer);
\draw[feedback] (repair.north) -- ++(0,7mm) -| node[pos=0.24, above, font=\sffamily\footnotesize] {if evidence is weak} (search.north);

% ---------------------------------------------------------------------------
% 4. Observability and evaluation
% ---------------------------------------------------------------------------
\node[box, below=16mm of verify] (trace) {Observability Traces\\{\small tools, scores, decisions}};
\node[box, right=of trace] (citations) {Citation Audit\\{\small quote-to-claim links}};
\node[orange, right=of citations] (ablation) {Ablation Studies\\{\small tool and graph budgets}};
\node[redbox, right=of ablation] (eval) {Evaluation Harness\\{\small golden, adversarial, human review}};

\begin{pgfonlayer}{background}
\node[stage, fit=(trace)(citations)(ablation)(eval), label={[stage title]above:{4. Measurement and Paper Evaluation}}] (stage4) {};
\end{pgfonlayer}

\draw[arrow] (answer.south) |- (trace.north);
\draw[arrow] (trace) -- (citations);
\draw[arrow] (citations) -- (ablation);
\draw[arrow] (ablation) -- (eval);

% Side grouping labels, matching the staged style of the reference figure.
\draw[decorate, decoration={brace, amplitude=7pt}, line width=0.7pt, draw=ink]
  ($(stage1.north west)+(-8mm,0)$) -- ($(stage3.south west)+(-8mm,0)$)
  node[midway, left=10mm, side label] {Agentic RAG\\Operation};

\draw[decorate, decoration={brace, amplitude=7pt}, line width=0.7pt, draw=ink]
  ($(stage4.north west)+(-8mm,0)$) -- ($(stage4.south west)+(-8mm,0)$)
  node[midway, left=10mm, side label] {Evaluation\\and Analysis};

\end{tikzpicture}
\end{document}
